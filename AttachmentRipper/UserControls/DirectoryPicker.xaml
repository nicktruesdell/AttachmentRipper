<UserControl x:Class="AttachmentRipper.UserControls.DirectoryPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AttachmentRipper.UserControls"
             mc:Ignorable="d">
    <DockPanel Margin="5,0,5,0">
        <Button Margin="0" Padding="0" DockPanel.Dock="Right" Width="20" Click="BrowseFolder">...</Button>
        <TextBox Height="Auto" HorizontalAlignment="Stretch" DockPanel.Dock="Right" 
           Text="{Binding Path=Text, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}" />
    </DockPanel>
</UserControl>
